# CPCL GPU

A repository to calculate the covariance matrix of the catalog-based pseudo-Cell (pseudo-Cl) estimator, using GPU acceleration.

## Contents

- `cpcl_paper_catalog.ipynb` — Main notebook for the catalog-based pseudo-Cell analysis and covariance calculations.
- `brute_cov.py` — Brute-force covariance matrix computation.

